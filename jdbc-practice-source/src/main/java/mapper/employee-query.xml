<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment/>
    <entry key="selectEmp">
        SELECT E.EMP_NAME,D.DEPT_TITLE,J.JOB_NAME
        FROM EMPLOYEE
        WHERE EMP_ID = ?
    </entry>
    <entry key="insertEmp">
        INSERT INTO TBL_MENU(E.EMP_ID,E.EMP_NAME, EMP_NO, EMAIL, PHONE, DEPT_CODE, JOB_CODE, SAL_LEVEL, SALARY, BONUS, MANAGER_ID, HIRE_DATE, ENT_DATE, ENT_YN)
        VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)
    </entry>
    <entry key = "deleteMenu">
        DELETE
        FROM EMPLOYEE
        WHERE EMP_NAME = ?

    </entry>
    <entry key="updateEmp">
        UPDATE EMPLOYEE
        PHONE  = ?
        ,EMAIL = ?
        ,DEPT_CODE = ?
        ,SALARY = ?
        ,BONUS = ?
        WHERE EMP_ID = ?
    </entry>
</properties>